[build-system]
requires = ["hatchling >= 1.26"]
build-backend = "hatchling.build"

[project]
name = "greeter"
requires-python = ">=3.14"
version = "0.0.0"

[project.scripts]
greeter = "greeter.main:main"